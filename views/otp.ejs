<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Verify OTP</title>
  <link rel="stylesheet" href="/css/otp.css" />
</head>
<body>
  <div class="container">
    <h2>Enter OTP</h2>
    <form id="otpForm" method="POST" action="/api/auth/verify-otp">
        <label for="otp">Enter 6-digit OTP</label>
        <input type="text" id="otp" name="otp" maxlength="6" required placeholder="Enter OTP" />

        <input type="hidden" name="email" value="<%= email %>" />
  
        <button type="submit">Verify</button>
    </form>

    <form id="resendForm" method="POST" action="/api/auth/resend-otp">
    <input type="hidden" name="email" value="<%= email %>" />
    <p style="text-align: center; margin-top: 10px;">
        Didn't receive the OTP? <button type="submit" class="link-button">Resend OTP</button>
    </p>
    </form>
</div>
</body>
</html>
